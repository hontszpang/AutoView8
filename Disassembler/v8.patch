diff --git a/src/objects/bytecode-array.cc b/src/objects/bytecode-array.cc
index fd1d8f33432..c8255c8dd01 100644
--- a/src/objects/bytecode-array.cc
+++ b/src/objects/bytecode-array.cc
@@ -4,6 +4,7 @@
 
 #include "src/objects/bytecode-array.h"
 
+#include <cstdlib>
 #include <iomanip>
 
 #include "src/codegen/handler-table.h"
@@ -138,21 +139,55 @@ void BytecodeArray::Disassemble(Handle<BytecodeArray> handle,
   }
 #endif
 
-  os << "Handler Table (size = " << handle->handler_table()->length() << ")\n";
+  int max_handler_table =
+      std::getenv("V8DASM_MAX_HANDLER_TABLE_BYTES") == nullptr
+          ? 4096
+          : std::atoi(std::getenv("V8DASM_MAX_HANDLER_TABLE_BYTES"));
+  int handler_table_length = 0;
+  bool handler_table_available = false;
+  PtrComprCageBase cage_base = GetPtrComprCageBase(*handle);
+  Tagged<Object> maybe_handler_table = handle->raw_handler_table(cage_base);
+  if (IsTrustedByteArray(maybe_handler_table)) {
+    handler_table_length = TrustedByteArray::cast(maybe_handler_table)->length();
+    handler_table_available = true;
+  }
+
+  os << "Handler Table (size = " << handler_table_length;
+  if (!handler_table_available) os << ", unavailable";
+  if (handler_table_length > max_handler_table) os << ", skipped";
+  os << ")\n";
 #ifdef ENABLE_DISASSEMBLER
-  if (handle->handler_table()->length() > 0) {
+  if (handler_table_available && handler_table_length > 0 &&
+      handler_table_length <= max_handler_table) {
     HandlerTable table(*handle);
     table.HandlerTableRangePrint(os);
   }
 #endif
 
-  Tagged<TrustedByteArray> source_position_table =
-      handle->SourcePositionTable();
-  os << "Source Position Table (size = " << source_position_table->length()
-     << ")\n";
+  int max_source_position_table =
+      std::getenv("V8DASM_MAX_SOURCE_POSITION_TABLE_BYTES") == nullptr
+          ? 4096
+          : std::atoi(std::getenv("V8DASM_MAX_SOURCE_POSITION_TABLE_BYTES"));
+  int source_position_table_length = 0;
+  bool source_position_table_available = false;
+  Tagged<Object> maybe_source_position_table =
+      handle->raw_source_position_table(kAcquireLoad);
+  if (IsTrustedByteArray(maybe_source_position_table)) {
+    source_position_table_length =
+        TrustedByteArray::cast(maybe_source_position_table)->length();
+    source_position_table_available = true;
+  }
+  os << "Source Position Table (size = " << source_position_table_length;
+  if (!source_position_table_available) os << ", unavailable";
+  if (source_position_table_length > max_source_position_table) {
+    os << ", skipped";
+  }
+  os << ")\n";
 #ifdef OBJECT_PRINT
-  if (source_position_table->length() > 0) {
-    os << Brief(source_position_table) << std::endl;
+  if (source_position_table_available && source_position_table_length > 0 &&
+      source_position_table_length <= max_source_position_table) {
+    os << Brief(TrustedByteArray::cast(maybe_source_position_table))
+       << std::endl;
   }
 #endif
 }
diff --git a/src/flags/flags.cc b/src/flags/flags.cc
index 7940f397fb0..f3f48fd4c8c 100644
--- a/src/flags/flags.cc
+++ b/src/flags/flags.cc
@@ -12,6 +12,7 @@
 #include <cstring>
 #include <iomanip>
+#include <iostream>
 #include <set>
 #include <sstream>
 
 #include "src/base/functional.h"
@@ -508,6 +509,9 @@ uint32_t ComputeFlagListHash() {
 #endif
 
   std::string args(modified_args_as_string.str());
+  std::cout << "[v8dasm] ComputeFlagListHash input_len=" << args.size()
+            << " input=" << args << "\n"
+            << std::flush;
   // Generate a hash that is not 0.
   uint32_t hash = static_cast<uint32_t>(base::hash_range(
                       args.c_str(), args.c_str() + args.length())) |
diff --git a/src/objects/string.cc b/src/objects/string.cc
index 3bfb418520a..72852f678fe 100644
--- a/src/objects/string.cc
+++ b/src/objects/string.cc
@@ -659,13 +659,6 @@ void String::StringShortPrint(StringStream* accumulator) {
   accumulator->Add("<String[%u]: ", len);
   accumulator->Add(PrefixForDebugPrint());
 
-  if (len > kMaxShortPrintLength) {
-    accumulator->Add("...<truncated>>");
-    accumulator->Add(SuffixForDebugPrint());
-    accumulator->Put('>');
-    return;
-  }
-
   PrintUC16(accumulator, 0, len);
   accumulator->Add(SuffixForDebugPrint());
   accumulator->Put('>');
diff --git a/src/snapshot/code-serializer.cc b/src/snapshot/code-serializer.cc
index 23463d26b63..043101d1d53 100644
--- a/src/snapshot/code-serializer.cc
+++ b/src/snapshot/code-serializer.cc
@@ -4,6 +4,7 @@
 
 #include "src/snapshot/code-serializer.h"
 
+#include <iostream>
 #include <memory>
 
 #include "src/base/logging.h"
@@ -473,6 +474,18 @@ MaybeHandle<SharedFunctionInfo> CodeSerializer::Deserialize(
     return MaybeHandle<SharedFunctionInfo>();
   }
 
+  std::cout << "\nStart SharedFunctionInfo\n";
+  std::cout << "<SharedFunctionInfo>\n";
+  if (result->HasBytecodeArray()) {
+    std::cout << "Start BytecodeArray\n";
+    result->GetActiveBytecodeArray(isolate)->Disassemble(std::cout);
+    std::cout << "End BytecodeArray\n";
+  } else {
+    std::cout << "[v8dasm] SharedFunctionInfo has no active bytecode array\n";
+  }
+  std::cout << "End SharedFunctionInfo\n";
+  std::cout << std::flush;
+
   // Check whether the newly deserialized data should be merged into an
   // existing Script from the Isolate compilation cache. If so, perform
   // the merge in a single-threaded manner since this deserialization was
@@ -687,10 +700,7 @@ SerializedCodeData::SerializedCodeData(const std::vector<uint8_t>* payload,
 SerializedCodeSanityCheckResult SerializedCodeData::SanityCheck(
     uint32_t expected_ro_snapshot_checksum,
     uint32_t expected_source_hash) const {
-  SerializedCodeSanityCheckResult result =
-      SanityCheckWithoutSource(expected_ro_snapshot_checksum);
-  if (result != SerializedCodeSanityCheckResult::kSuccess) return result;
-  return SanityCheckJustSource(expected_source_hash);
+  return SerializedCodeSanityCheckResult::kSuccess;
 }
 
 SerializedCodeSanityCheckResult SerializedCodeData::SanityCheckJustSource(
diff --git a/src/snapshot/deserializer.cc b/src/snapshot/deserializer.cc
index b68a4fdd1d5..4ed3f6a3a5b 100644
--- a/src/snapshot/deserializer.cc
+++ b/src/snapshot/deserializer.cc
@@ -265,7 +265,6 @@ Deserializer<IsolateT>::Deserializer(IsolateT* isolate,
 #ifdef DEBUG
   num_api_references_ = GetNumApiReferences(isolate);
 #endif  // DEBUG
-  CHECK_EQ(magic_number_, SerializedData::kMagicNumber);
 }
 
 template <typename IsolateT>
